# React Native Client

This client uses Expo for the fastest demo setup.

## Install

```bash
npm install
```

## Run

```bash
npm start
```

Default backend URL in `App.js`:

`http://10.0.2.2:8080/darija-translator/api/translate`

For a physical device, replace it with your computer's LAN IP.
